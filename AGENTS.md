# Agents

The agent instructions + the trigger map for this repo live in **[`CLAUDE.md`](CLAUDE.md)**
(single source of truth). Start there: the **"Agent trigger map (second brain)"**
section routes you to `docs/GOTCHAS.md` (footguns) and `docs/decisions/` (ADRs)
at the moment each matters.
